
server:
    port: 8888

spring:
    application:
        name: SpringCloudConfigServer
    profiles:
        active: native
    cloud:
        config:
            server:
                native:
                    search-locations: classpath:/config_repo
#                git:
#                    uri: file://classpath:/config_repo
                    #file://${user.home}/config_repo
                    #file://${HOME}/config-repo
                    #classpath:/config
#                    clone-on-start: true
    security:
        basic:
            enabled: false
        user:
            name: root
            password: blabla
logging:
    level:
        root: INFO





